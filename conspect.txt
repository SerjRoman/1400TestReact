Public API(Barrel file) - 


src - source code(исходный код)

Под каждый компонент создается папка с названием компонента.
В этой папке мы создаем все что относится к работе компонента:
    1. Типизацию(пропсы, вспомогательные типы)
    2. Файл со стилями(*.module.css)
    3. Public API файл(Barrel File) - index.ts предоставляющий компонент из папки
    4. Любые доп. функции(компоненты, хуки и тд), которые нужны только в этом компоненте
Нейминг в приложении:
1. Файлы: 
name1-name2-name3.расширение

Примеры:
ProductList компонент = product-list.tsx. 
Стили к ProductList = product-list.module.css
Типизация к ProductList = product-list.types.ts

2. Папки:
name1-name2-name3

Примеры:
product-list
select-category

Слои приложения:

app - отвечает за сборку всего и вся из приложения и запуск его (кроме index.tsx)
pages - страницы приложения(сборка components и shared для создания страниц)
components - специальные компоненты, реализующие конкретные UI(User Interface) блоки(product-list, select-category, cart-list..)
shared(общее) - переиспользуемые функции, UI элементы, типы и тд(кнопки, текст, модалки, стандартные формы и тд)
assets - статические файлы приложения(картинки, иконки, гифки, шрифты...)




                app
        pages pages pages
    components components components
shared shared shared shared shared shared


При разделении кода архитектурно следуйте этим принципам:
1. Однонаправленность зависимостей (пример: 
    import в модуле User из другого модуля не хорошо - ☹️👎
    Pages-ы зависят от компонентов но при этом компоненты не зависят от Pages - 😃👍
    )
2. Стабильность, стремитесь к четкому разделению стабильных и нестабильных модулей, классов, компонентов, приложений и т.д.
    Пример:
        Components в самом низу, потому он должен быть самым стабильным, при этом Pages выше потому может быть менее стабильный 
    Стабильность - это стремление к созданию наименьшего числа причин для изменений класса/модуля/компонента
3. Дополнительно - Стремитесь к такому коду, в который легко добавлять новый функционал, не изменяя старый (стабильность v2.0👍)
4. Принцип единственной отвественности(Single Responsibility Principle) - стремитесь к тому, чтобы модуль/класс/компонент менялся только по одной причине (стабильность v3.0🔥)

SOLID

S - Single Responsibility Principle(SRP)
O - Open/Closed
L
I
D - Dependency Inversion - инверсия зависимостей. Компоненты, модули, функции, классы должны зависить от абстракций, а не от конкретных реализаций и правил

Порядок действий для создания страницы:
1. Типизация сущностей, моделей (продуктов, категорий и тд), абстракция.
2. Реализация API-запросов, создания хуков, обработчиков ошибок API.
3.1 Создание папки страницы и нужных ему файлов для реализации страницы (компонент, ..module.css, index.ts)
3.2 Добавление роутинга для этой страницы
3.3 Добавление перехода на эту страницу(кнопки с навигацией, гипер-ссылки)
3.4 Добавление статических файлов (Картинки, видео, гифки и тд) 
4. Базовая разметка и базовые стили
5. Добавление API, добавление функционала к страницы. Реализация всей задачи этой страницы
6. Доделываем стили, разметку, функционал если необходимо
После каждого пункта, если возможно, проверить как работает тот или иной шаг
